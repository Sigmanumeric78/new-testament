# Legacy Supabase Migration Utilities

These files are retained only for historical recovery and audit of the completed Supabase Storage to MongoDB Atlas/GridFS migration.

They are not part of the production runtime, are not imported by `backend/`, and are not copied into the Lambda image.

Do not add Supabase dependencies back to `backend/requirements.txt` for these utilities.

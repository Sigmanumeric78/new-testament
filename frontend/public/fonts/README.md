Place Anurati font files here for consistent rendering:

- `Anurati-Regular.woff2` (preferred)
- `Anurati-Regular.otf` (fallback)

The frontend already references these paths via `@font-face` in `src/styles.css`.
